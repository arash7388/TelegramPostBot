namespace Common
{
    public enum ImageSource
    {
        ProductCategories,
        Products,
        Posts
    }

    public enum MessageType
    {
        Info,
        Warning,
        Error,
        Success
    }

    public enum InOutType
    {
        In=1,
        Out=2
    }

    public enum ProductionQuality
    {
        Bad = 1,
        Normal = 2,
        Good=3
    }
}